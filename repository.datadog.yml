---
schema-version: v1
kind: mergegate
rules:
  - require: commit-signatures
    excluded_emails:
      - 'noreply@anthropic.com' # Claude bot
      - 'team-workspaces@datadoghq.com' # DataDog Workspaces Bot
